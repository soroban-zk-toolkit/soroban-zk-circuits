# Contributing

Branch model: contributors push to `develop`; `main` is protected.

Commit convention: Conventional Commits (type(scope): description)

Use `make` or `npm run build` to compile circuits. Add tests where appropriate.
